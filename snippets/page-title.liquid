<div class="mb-4 mb-sm-5 text-center page-title">
    <h1 class="mb-0 gkit-h2 text-center">{{ title }}</h1>
    {% unless summary == blank %}
        <p class="mt-2 mt-md-3 mb-0 gkit-p-20">{{ summary }}</p>
    {% endunless %}
</div>
